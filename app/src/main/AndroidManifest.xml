<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.surge">
    <uses-permission android:name="android.permission.INTERNET"> </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".Waterlevel"></activity>
        <activity android:name=".ForgetPass" />
        <activity android:name=".MainLogin" />
        <activity android:name=".DContacts" />
        <activity android:name=".Notices" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
        <activity android:name=".SafeplacesMain" />
        <activity android:name=".ContactMain" />
        <activity android:name=".Home" />

        <activity android:name=".departmentContacts" />
        <activity android:name=".donation" />
        <activity android:name=".uploader" />
        <activity android:name=".safeplaces" />
        <activity android:name=".Register" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
    </application>

</manifest>